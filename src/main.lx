@module "main"

@use "std_libc" as libc

@use "layer_tests" as layer_tests
@use "gate_tests" as gate_tests

pub const main -> fn () int {
    libc::srand(libc::time(cast<*int>(0)));

    layer_tests::layer_run();
    gate_tests::gate_run();
    
    return 0;
}